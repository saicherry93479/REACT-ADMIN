import React, { useEffect } from "react";
import Form from "./Form";

const NewPost = () => {
  useEffect(() => {}, []);
  return (
    <div>
      <Form post="new" />
    </div>
  );
};

export default NewPost;
